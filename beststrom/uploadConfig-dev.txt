
// uploadConfig-dev.txt
option echo off                       // Toggles echoing of command being executed.
option batch on                       // Any choice prompt is replied and any input prompt is cancelled
option confirm off                    // Toggle confirmations (overwrite, etc)
open best@beststrompris.no:P2oU6#o@rrna@ftp.beststrompris.no          // Your FTP credentials and host
cd "strompris"                       // Go to a remote folder (if needed)
rmdir "static"                        // Delete existing remote folder
lcd "build"                           // Go to local "build" folder
put -nopermissions -nopreservetime *  // Upload your "build" folder to remote "foldername"
exit                                  // Exit winscp